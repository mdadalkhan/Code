/* 
 * Coordinate System 
 * 01. Document Reference - fixed all the time
 * 02. View Port Reference =  Client Area
 * 03. Screen , Page , Client , Offset ,  Window, Element , Scroll
 */


/*
 * Element << Padding << Border << Margin Box Model.
 * Box-sizing: border-box | Content Box
 * Element Width  = width + padding + border = actual Width
 * Element Height = Height + padding + border = actual Height
 * box-sizing: border-box 
 * Actual Width = 200px
 * within 200px padding + border + content will be fit
 * but in content-box : border+ padding is excluded
 */



/*
 * HTMLElements - blocked elements
 * 01. width  = actual width  -(padding+border)
 * 02. height = actual height - (padding+border)
 * 03. for border-box
 *     width = element width + padding + border
 *     height = element height + padding +border
 * 04. offsetHeight = element Height relative to its parent element
 * 05. offsetWidth  = element Width relative to its parent element
 * 06. offset parent = parent element from where width is calculated
 * 07. offsetLeft = distance from (Parent's Left Border - Element's Left Border)
 * 08. offsetTop  = distance from (Parent's Top border - Element's top border)
 *
 *
 *
 */



/*
 * window > Document > View Port
 * 01. windowInnerHeight = 
 */



